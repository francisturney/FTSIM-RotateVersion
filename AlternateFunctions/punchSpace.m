function space = punchSpace(space,surface)
    
    s = size(surface);
    for i=1:s(1,1)
        if space(surface(i,1),surface(i,2)=0
            space(surface(i,1),surface(i,2))= i;
       
            